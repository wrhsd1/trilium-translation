{
  "$schema": "../node_modules/@lobehub/chat-plugin-sdk/schema.json",
  "api": [
    {
      "url": "https://test.988886.xyz/playground",
      "name": "drawing",
      "description": "根据用户要求进行画图。",
      "parameters": {
        "properties": {
          "description": {
            "description": "The user input text describing the scene, sent as the `description` field in the request body.",
            "type": "string"
          },
          "params": {
            "description": "Additional parameters like width, height, and seed for image generation.",
            "type": "string"
          }
        },
        "required": ["description"],
        "type": "object"
      },
      "method": "POST",
      "content-type": "application/json",
      "request": {
        "body": "{ \"description\": \"{description}\" }"
      }
    }
  ],
  "identifier": "pollinations-drawing111",
  "meta": {
    "avatar": "🎨",
    "tags": ["drawing"],
    "title": "AI Drawing Service",
    "description": "Generates images based on user input through a local AI service."
  },
  "type": "markdown",
  "version": "1"
}
