{
  "$schema": "../node_modules/@lobehub/chat-plugin-sdk/schema.json",
  "api": [
    {
      "url": "https://test.988886.xyz/playground",
      "name": "drawing",
      "description": "根据用户要求进行画图。",
      "parameters": {
        "properties": {
          "description": {
            "description": "{description} is the user input text describing the scene. Ensure this text is in English and properly URL-encoded.",
            "type": "string"
          },
          "params": {
            "description": "{params} defines additional parameters such as width, height, and seed for image generation. These should be URL-encoded.",
            "type": "string"
          }
        },
        "required": ["description"],
        "type": "object"
      }
    }
  ],
  "identifier": "pollinations-drawingtest",
  "meta": {
    "avatar": "🎨",
    "tags": ["drawing"],
    "title": "AI Drawing Service",
    "description": "Generates images based on user input through a local AI service."
  },
  "type": "markdown",
  "version": "1"
}
